# SvelteKit public env vars (must use the PUBLIC_ prefix to be exposed to the browser).
# Optional override of the API base URL the typed fetch client targets.
# Defaults to '/api' (proxied to the FastAPI service by Vite in dev or nginx in prod).
# PUBLIC_API_BASE=/api
